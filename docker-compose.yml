version: "3.4"

services:
  api:
    build:
      context: .
      target: development
    volumes:
      - ./:/usr/src/app
      - /usr/src/app/node_modules # Don't sync this as bind mount from above
    ports:
      - 5000:5000
    command: npm run dev
      